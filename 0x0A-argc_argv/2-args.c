#include <stdio.h>

/**
 * main-print all arguments recieved
 * @argc:argument count
 * @argv:array of strings
 * Return:0
 */
int main(int argc, char **argv)
{
	int x;

	for (x = 0; x < argc; x++)
		printf("%s\n", argv[i]);

	return (0);
}
